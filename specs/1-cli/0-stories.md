# 01 - Command Line Interface (CLI) — Stories

## Context
The application run can be configured through arguments in the Command line interface. User will often start the application through a terminal.

---

## User Story 1 — show version (P1)
As a user, `pza-power-supply --version` display the version of the binary. 
